services:
  display-mbsport:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api.mbsport.lat}
    expose:
      - "80"
    restart: unless-stopped
